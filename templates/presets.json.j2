{
  "active": {{ ya_provider_presets_active|to_nice_json }},
  "presets": [
  {% for item in ya_provider_presets_active %}
    {
      "name": "{{ item }}",
      "exeunit-name": "{{ item }}",
      "pricing-model": "linear",
      "usage-coeffs": {{ ya_provider_presets_usage_coeffs|to_json }}
    {% if not loop.last %}
    },
    {% else %}
    }
    {% endif %}
  {% endfor %}
  ]
}
